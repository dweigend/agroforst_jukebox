import { IManager } from '../types';

export class RoundUIManager implements IManager {
  constructor() {
    this.initializeContainer();
  }

  private initializeContainer(): void {}
  dispose(): void {}
}
